Docker wrapper for javascript-obfuscator, that accepts local file and outputs to STDOUT.

# First build image:
```docker build . -t obfuscator```

# Use local helper script to obfuscate:
```./obfuscate.sh file_to_obfuscate.js```

